apiVersion: v1
kind: Pod
metadata:
  name: pvc-pod
  namespace: admin-tp2
spec:
  containers:
    - name: app-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http"